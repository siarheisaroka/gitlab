version: '3'
services:
  nginx:
    image: $DOCKER_USER_NAME/$IMAGE_NAME:$IMAGE_TAG
    container_name: nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - archive:/usr/share/nginx/html/archive
volumes:
  archive:
      
